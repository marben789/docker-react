sudo: required
language: generic 
services:
    - docker
    
beofore_install:
    - docker build -t mb/docker-react -f Dockerfile.dev .
    
script:
  - docker run -e CI=true mb/docker-react npm run test    
